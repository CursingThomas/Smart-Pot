#include <Arduino.h>
#include "parsingProcesstring.h"

parsingProcesstring::parsingProcesstring(int y)
{
    y = y;
    x = x;

}

char parsingProcesstring::getParsedobject()
{
    char x = x + y;

    return x;
}